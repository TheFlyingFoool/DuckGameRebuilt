/*namespace DuckGame
{
    [ClientOnly]
    public class DeathCaliber : Bullet
    {
        public DeathCaliber(float xval, float yval, AmmoType type, float ang = -1f, Thing owner = null, bool rbound = false, float distance = -1f, bool tracer = false, bool network = true) : base(xval, yval, type, ang, owner, rbound, distance, tracer, network)
        {
        }
        public override void Initialize()
        {
            color = Color.Red;
            base.Initialize();
        }
        public override void Draw()
        {

            base.Draw();
        }
        public override void Update()
        {
            base.Update();
        }
    }
}*/